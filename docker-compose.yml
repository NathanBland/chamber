version: "3.2"
services:
  app:
    build: .
    image: chamber:1
    depends_on:
      - postgres
    ports:
      - "3000:3000"
      - "9229:9229"
  postgres:
    image: postgres:10-alpine
    environment:
      POSTGRES_PASSWORD: postgres